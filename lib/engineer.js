const Employee = require ('./employee');

class Engineer extends Employee {
    contructor (officeNumber) {
        this.officeNumber = officeNumber;
    }


getRole() {
    const Engineer = new Enginner("github");
    if(this.github === true) {
    console.log (`Employee:${this.Engineer}`);
    }
    //returns Employee
}

getGitHub(){
    if (this.github) {
        console.log (`Github: https://github.com/${github}`)
    } else {null
        
    }
}


}
