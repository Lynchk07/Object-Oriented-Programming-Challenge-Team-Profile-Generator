const Employee = require ('./employee');

class Intern extends Employee {
    contructor (school) {
        this.school = school;
    }


getRole() {
    const Intern = new Intern(school);
    if(this.school === true) 
    {
    console.log (`Employee:${this.Intern}`);
    } 
    //returns Employee
}

getSchool(){
    if (school) {
        console.log (`School: ${this.school}`)
    } else {null
        
    }
}

}